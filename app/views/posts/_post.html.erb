<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h5>Posted by <%= link_to post.user.name, user_path(post.user) %></h5>  <!-- showing the email of the user who made the post -->
  <h6><%= pluralize(post.view, "view") %></h6>
  <div >
    <%= post.body %>
  </div>

</div>

 